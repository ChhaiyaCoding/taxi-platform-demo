# Tabi — demo build

Public demo of a Cambodian inter-province taxi booking app.
Everything here is generated output; the source lives in a private repository.

- `app/` — Flutter web build
- `prototype/` — interactive HTML design prototype

Sign in with any phone number and the code `123456`.
No real data, no real payments.
